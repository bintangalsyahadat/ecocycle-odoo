[options]
addons_path = /mnt/extra-addons
data_dir = /var/lib/odoo

; DB config — override via Railway env vars (HOST, PORT, USER, PASSWORD)
db_host = False
db_port = False
db_user = False
db_password = False
